while(lines=readline()){
  var T = parseInt(lines[0]);
  var s = lines[1,lines.length-1];
  if(T==s.length){
    function games(arr){
      for(i=0;j<arr.length;i++){
       newArr = pasreInt(arr[i])
       for(j=0;j<newArr.length;j++){
        while(){
          
        }
        
       }
      }
    }
  }
}
